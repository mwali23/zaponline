"use client";
import { useState } from "react";
import { CheckCircle2, MapPin } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

export function ReportForm(){const [done,setDone]=useState(false);const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const supabase=createClient();if(!supabase){setDone(true);setMessage("Demo report captured locally. Connect Supabase to persist community reports.");return;}const {data:{user}}=await supabase.auth.getUser();if(!user){setMessage("Please sign in before submitting so reports can build a trusted reputation.");return;}const districtName=String(form.get("district")??"");const {data:district,error:districtError}=await supabase.from("districts").select("id").eq("name",districtName).single();if(districtError||!district){setMessage("That district is not configured yet.");return;}const {error}=await supabase.from("outage_reports").insert({reporter_id:user.id,district_id:district.id,area_label:form.get("area"),status:form.get("status"),description:form.get("description")});if(error)setMessage(error.message);else{setDone(true);setMessage("Thanks—your report is now awaiting community confirmation.");}}return <form className="form-card" onSubmit={submit}>{done?<div style={{textAlign:"center"}}><div className="step-icon"><CheckCircle2/></div><h2>Report received</h2><p>{message}</p></div>:<><div className="step-icon" style={{width:58,height:58,margin:"0 0 20px"}}><MapPin/></div><h2>What&apos;s the power status?</h2><p>Your report is approximate and never publishes your exact address.</p><div className="form-grid"><div className="field"><label htmlFor="district">District</label><select id="district" name="district" className="select" required><option value="">Choose district</option>{["Kitwe","Ndola","Chingola","Mufulira","Luanshya","Kalulushi","Lufwanyama","Masaiti","Mpongwe","Chililabombwe"].map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label htmlFor="status">Current status</label><select id="status" name="status" className="select" required><option value="outage">Power is out</option><option value="powered">Power is on</option><option value="unstable">Power is unstable</option></select></div><div className="field full-span"><label htmlFor="area">Neighbourhood / landmark</label><input id="area" name="area" className="input" placeholder="e.g. Riverside near the clinic"/></div><div className="field full-span"><label htmlFor="description">Anything else? (optional)</label><textarea id="description" name="description" className="textarea" rows={4} placeholder="Flickering, restoration time, affected streets…"/></div></div><div className="notice">Reports are scored using account history, recency, location precision and independent confirmations. False reports may lead to account restrictions.</div><button className="button button-primary full">Submit community report</button>{message&&<div className="notice" role="status">{message}</div>}</>}</form>}
