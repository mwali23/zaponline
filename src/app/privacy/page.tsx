export default function PrivacyPage(){return <Policy title="Privacy at ZAP" sections={[['What we collect','Account details, approximate report location, marketplace activity and technical security logs. Exact addresses are not shown on the public outage map.'],['Why we use it','To operate accounts, calculate report confidence, match service requests, prevent abuse and measure impact.'],['Your choices','You can manage alerts and marketing consent separately. Production launch requires a documented deletion and data-access process.']]}/>}
function Policy({title,sections}:{title:string;sections:string[][]}){return <><section className="page-hero"><div className="container"><span className="kicker">Legal baseline</span><h1>{title}</h1><p>This product draft is not a substitute for counsel. Review against Zambian data-protection and consumer laws before launch.</p></div></section><section className="content-section"><div className="container" style={{maxWidth:760}}>{sections.map(([h,p])=><div key={h}><h2 style={{fontSize:28}}>{h}</h2><p>{p}</p></div>)}</div></section></>}
