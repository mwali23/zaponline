import { FileCheck2, ShieldCheck, TriangleAlert } from "lucide-react";
export default function SafetyPage(){return <><section className="page-hero"><div className="container"><span className="kicker">Marketplace trust</span><h1>Safety is a workflow, not a badge.</h1><p>ZAP creates a shared record of who agreed to what. Verification reduces uncertainty, but customers should still make informed decisions.</p></div></section><section className="content-section"><div className="container info-grid">{[[ShieldCheck,"Provider review","Identity, business information and submitted qualifications may be checked before verification."],[FileCheck2,"Written scope","Quotes, milestones, payment records and completion evidence stay attached to the job."],[TriangleAlert,"Know the limits","ZAP does not guarantee providers, utility data or restoration estimates. Report emergencies to local authorities."]].map(([Icon,title,copy])=>{const C=Icon as typeof ShieldCheck;return <article className="info-card" key={title as string}><C/><h3>{title as string}</h3><p>{copy as string}</p></article>})}</div></section></>}
