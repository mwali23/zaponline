import Link from "next/link";
import { ArrowRight, BarChart3, Building2, FileCheck2, HeartHandshake } from "lucide-react";
import { impactProjects } from "@/lib/demo-data";

export default function ImpactPage(){return <><section className="page-hero night"><div className="container"><span className="kicker light">ZAP Impact</span><h1>Fund power access you can follow.</h1><p>A transparent project workspace for NGOs, companies, diaspora groups and communities—from verified need through installation and measured outcomes.</p><a className="button button-primary" href="mailto:partners@zaponline.org">Discuss a partnership <ArrowRight size={17}/></a></div></section><section className="content-section"><div className="container"><div className="section-heading"><span className="kicker">Community projects</span><h2>Practical energy, visible progress.</h2></div><div className="info-grid">{impactProjects.map(project=><article className="info-card" key={project.title}><span className="kicker">{project.location}</span><h3 style={{fontSize:22}}>{project.title}</h3><p>Expected reach: {project.people}</p><div className="progress"><i style={{width:`${project.raised}%`}}/></div><p style={{display:"flex",justifyContent:"space-between"}}><strong>{project.amount}</strong><span>of {project.goal}</span></p><Link className="arrow-link" href="/sign-in?next=donate">View evidence & milestones <ArrowRight size={16}/></Link></article>)}</div></div></section><section className="section section-tint"><div className="container"><div className="section-heading centered"><span className="kicker">Accountability by design</span><h2>A clearer line from funding to impact.</h2></div><div className="info-grid">{[[Building2,"Verified community need","Local organizations document the site, beneficiaries and ownership plan."],[FileCheck2,"Milestone-based delivery","Funds can be tied to reviewed procurement, installation and acceptance stages."],[BarChart3,"Outcome reporting","Track uptime, people served, maintenance events and project updates."],[HeartHandshake,"Shared stewardship","Community, implementer and sponsor each have clear responsibilities."]].map(([Icon,title,copy])=>{const C=Icon as typeof Building2;return <article className="info-card" key={title as string}><C/><h3>{title as string}</h3><p>{copy as string}</p></article>})}</div></div></section></>}
